{
  "ground_truth": {
    "date": "2024-11-14",
    "amount": 365.00,
    "vat_amount": 74.16,
    "odometer_km": null,
    "company": "Hyvälän autolasipalvelu",
    "invoice_number": "14501"
  },
  "expected_extraction": {
    "parsing": {
      "required_fields": ["date", "amount", "company"],
      "warning_if_missing": ["invoice_number"],
      "optional_fields": ["vat_amount"]
    },
    "llm_extraction": {
      "required_fields": ["date", "amount", "company"],
      "warning_if_missing": ["invoice_number"],
      "optional_fields": ["vat_amount"]
    },
    "final_data": {
      "required_fields": ["date", "amount", "company"],
      "warning_if_missing": ["invoice_number"],
      "optional_fields": ["vat_amount"]
    }
  }
}
